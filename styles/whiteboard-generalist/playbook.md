# 风格包 · Whiteboard Generalist（公用白板 · 生产兜底模板）

> 从 KuleshovAgent 迁入（2026-07-24）。机械硬门在本目录 `contract.json`（护栏面持有，本文不复述阈值）；本文管创作方法，审美要求交 Judge。
> 🔴 **本包不是第三个风格 SKU，是生产兜底模板。** 它**保下限，不承担清晰的用户可感知风格承诺**——这是定位，不是缺陷。
> 当选题不满足任一专用包的叙事前提、或路由置信度不足时用它。

## 0. 档案

- 状态：**兜底模板（generalist fallback）**——职责是"接住其它包接不住的"，不走考片制上架（考片挣上限，兜底保下限）。
- 适用：其他 / 混合 / 低置信度 / 未知公测输入；用户诉求超出现有风格包预期时的默认收口。
- **路由条件**（机器版在 `capability.json`，规程 `styles/routing.md`）：`status: fallback` → **不参与竞争打分**。它不会因为"什么都沾一点"挤掉专用包，也不会因为分低被漏掉；路由器永远把它作为最后一行兜底候选输出。落本包的三种成因：① 专用包全被硬规则排除；② 最高契合度不足；③ **最高分的包不承担这条片的理解任务**（含理解任务判为 `unknown`）。
- **落本包时要做的两件事**：把路由器输出的 `gap` / 排除理由原样记进 `ledger.decisions`；若 `gap` 非空（理解任务无包认领），登记到 `styles/routing.md` §7 空位表——兜底出的每一条片都是"该补哪个包"的证据。
- 唯一硬不兼容：写实真人重演（真人只允许符号化角色 / 叙事替身——三包共同规则）。
- 交付承诺类型：由本片合同与所选生成风格决定（见 §1）。
- 考片记录：无。

## 1. 链路配方（硬 · 三链路架构 2026-07-24）

**空白链路** = **生成风格八选一（同片单一）+ 实拍视频 + HTML + 图片 + 数字人**，元素可自由组合，唯一铁律是**生成镜风格一片一种、不得混**。

### 生成风格 registry（2026-07-28 从二选一扩到八选一）

| profile | 核心优势 | 最适合 | 出处 | 状态 |
|---|---|---|---|---|
| `pixel` 像素风 | 限定调色板 + 硬边栅格 | 角色重演、数据实物化、历史重演 | `.claude/skills/pixel-broll/` | ✅ 已冒烟 |
| `collage` 半调纸拼贴 | 高级编辑风氛围 b-roll | 抽象隐喻的口播间奏 | `.claude/skills/collage-broll/` | ✅ 已冒烟 |
| `object-theatre` 实物桌面剧场 | 熟悉物品形成意外隐喻 | 观点、反转、效率、职场、人性 | `.claude/skills/broll-studio/` | ⚠️ 静帧已验/视频未验 |
| `technical-diagram` 技术图解 | 把抽象机制讲清楚 | AI、Agent、工作流、系统机制 | 同上 | ⚠️ 静帧已验/视频未验 |
| `clay-miniature` 黏土微缩 | 荒诞、幽默、可变形 | 情绪、焦虑、冲突、轻观点 | 同上 | ✅ 已冒烟 |
| `felted-wool` 毛毡动画 | 柔软、温暖、有人情味 | 关系、陪伴、生活、品牌价值 | 同上 | ⚠️ 静帧已验/视频未验 |
| `popup-book` 立体书／分层景观 | 擅长展示路径与世界 | 成长、旅程、生态、流程 | 同上 | ⚠️ 静帧已验/视频未验 |
| `toy-world` 品牌产品静物 | 高级、统一、产品化 | 产品功能、平台、商业与科技 | 同上 | ⚠️ 静帧已验/视频未验 |

**这八套是材质语言，不是八个风格 SKU**——它们共用同一套三闸门、几何、引擎绑定与 QA，profile 只承载「材质语汇 / 运动动词 / 首帧规则 / 失败标准」四样。选型走 `python3 tools/broll-profile.py route "<文稿类型>"`（表在 `.claude/skills/broll-studio/routing.json`）。

- 开工 / 分镜阶段先在 `meta.style_notes.generated_style` 写死 profile id——选定后逐镜按该风词表执法，保证同片单一风。
  > ⚠️ `STYLE_LOCK` 机器门 molly 暂未覆盖（见 `contract.json` 的 `_molly_enforcement`）——**但串词现在有机器门**：`python3 tools/broll-profile.py lint <id> --file prompt.txt` 检查提示词有没有混入别的 profile 的签名词汇（否定式约束如 `No paper collage` 不算串词，那是主动划界）。「同片只有一种」仍靠纪律 + Judge。
- 各 profile 的工艺随风而变（pixel 走图先行像素锚点 + 归一层；collage 与其余五套走空色场首尾帧组装；**`popup-book` 例外，必须以合上的书起手**，空场会让空间凭空出现）。**选 pixel 即禁用 collage b-roll 声部**（其工艺就是拼贴）。所有 seedance 镜均**图先行**（≥1 静帧锚 + ≥10s 慢组装，见 `contract.json` 的 `traits.craft`）。
- **换画幅 / 换模型版本 / 大改 prompt 骨架后**：Gate 3 先只跑 1 条测试 clip 目检再排产，结论写回该 skill 的状态节。跳过 = 静默降级。**pixel / collage / clay-miniature 三套端到端已冒烟**；broll-studio 另五套 2026-07-28 按参考实图重写过提示词，静帧人眼已验、**视频层待验**（旧成片对应旧模板，不再背书）。16:9 只有 collage 验过。
- 实拍视频（`footage`）、静态图片（`gpt_image`）、数字人（`avatar`，须挂 portrait 锚点 + 音频先行）都可用；HTML 做简单排版，版式经 compose 门。

## 2. 定位（先读这段，防包名误导）

- **本包不是"白板画风"**——"白板"指的是**给你一块空白画布的质量下限模板**：一套与画面外观无关的生产纪律（每镜单一传播意图 / 参考资产 / 连续性 / 边界设计 / 统一手柄 / 实测验收），套在任何长尾选题上保证片子不塌底。
- **画面外观完全由选题决定**：做菜的片该有食物质感，讲财报的片该是数据版式——AI 视频 / 真实素材 / 图表 / 图片动效 / 数字人 / 文字包装自由混排。把任何选题都做成白底手绘涂鸦风，是对本包最常见的误读，视同模板味（Judge 专项直接打）。
- **唯一硬不兼容**：写实真人重演（真人只允许符号化角色 / 叙事替身，三包共同规则）。

## 3. 必须成立的东西（Judge 验收，合同只查声明在场）

全片必须有统一的色彩 / 字体 / 节奏**或**重复视觉手柄——开工时把手柄声明写进 `meta.style_notes.visual_handle`（合同机械门查在场性，是否成立交 Judge）。**换个选题仍完全成立的模板化画面 = 不合格**（Judge 模板味专项）。

## 4. 生产纪律（通用视频生产纪律的原则化，不机械照搬）

1. **锁定 brief**：开拍后不改目标；改目标 = 回 brief 重走。
2. **重复人物与关键物建参考资产**，固定引用映射（`@Image1 = …` 一经分配不换序）——见 produce SOP 锚点阶段（anchors）。
3. **显式维护跨片段连续性**：服装 / 朝向 / 道具位 / 光线逻辑逐镜检查。
4. **片段边界设计**：可读的结束构图 + 下一镜切入手柄。
5. **生成后 ffprobe 验证真实时长**（`tools/measure-render.py` 成片实测层做，自报与实测矛盾以实测为准）——按实测时长排片。

## 5. 素材配比（硬门 · storyboard 门前移拦截）

**纯 HTML/MG 动画（`provider=hyperframes`）按时长份额 ≤ 40%**；剩余 **≥60% 必须由真素材声部承担**，从下列自由混排：

- **AI 图片动效**（`image_motion`）：GPT-Image 生成图 + 运镜 / 视差，做示意图、场景、情绪空镜；
- **真实图片 / 真实视频**（`footage`）：Pexels / 档案实拍空镜、真实照片；
- **数字人**（`avatar`）：口播人设、旁白具象化；
- **seedance 视频**（`seedance` / `collage_broll`）：决定成片上限的高价值镜头、拼贴 b-roll。

这条在 **storyboard 阶段**由 `style.contract.plan` 门按声明的 `shots[].source.provider` 时长份额判定（`plan.provider_share.hyperframes.max`）——**花钱和出片前**就挡住"全 MG 幻灯片"。分镜时就要把镜头价值想清楚、主动把 6 成以上时长分给真素材，而不是默认全塞 hyperframes 等 render 期被打回。hyperframes 该承担的是：动态字幕 / 字卡、数据卡、UI 演示、过场衔接——不是整片主视觉。

## 6. 动效升级（"做得更炫酷 / 更有动效"落这里，不靠堆特效）

用户提"更炫酷 / 更有动效 / 像发布片"这类模糊诉求时，**不要随机叠效果**——走 compose 知识包的 `.claude/skills/produce/references/hyperframes-recipes.md`（F01–F16 few-shot）：

1. **先定叙事动作**：出现 / 替换 / 完成 / 比较 / 交互 / 进入另一个世界 / 展示并行规模；
2. **选 1 个主配方，最多叠 1 个辅助**（配方决策顺序见该文件 §2）；
3. **品牌变量只换皮肤，不改 motion grammar**：字体 / 颜色 / 圆角 / 纹理 / 资产按选题替换，时序结构照抄；
4. **炫酷 ≠ 模板**：套用后要能写清用了哪些配方、对应什么叙事动作、关键时间点——换个选题仍完全成立的模板化画面 = 不合格（Judge 模板味专项）。

引擎工艺（怎么写才不崩：属性 / 媒体 / 子 composition / 确定性）在 `.claude/skills/produce/references/hyperframes.md`，本包不复述，只负责"按选题气质选对表达、保住下限"。

## 7. 传播

- **搜索词先行**：选题 / 标题含 1 个用户真的会搜的词，长尾问句化优先。
- **钩子节律**：3 秒钩住 → 15 秒内亮核心承诺 → 每 15–30s 一个小高潮。
- **首帧海报化**：hook 镜首帧按海报标准设计（可直接充当封面资产）。
- 医疗 / 心理 / 法律等强监管选题：只讲风险 / 法规 / 机制，不给场景化专业建议；绝对化用语禁绝（广告法通杀项）。
- 引用平台机制数字必须标 [官方规则] / [社区共识] / [推测]。

## 8. 兜底路由

EP 在 produce 开拍提问（`SKILL.md` §1.3）跑 `python3 tools/route-style.py`（三层路由：硬规则排除 → 能力卡打分 → 置信兜底）→ 置信 `low` 落本包。**不许为了凑一个专用包去改路由输入特征**——路由器把被排除的包和原因都打出来了，改输入等于伪造证据。全部专用包被排除是正常结果，不是停点问人的理由（自主模式照常出片，把明细记进 ledger）。
