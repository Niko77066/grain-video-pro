# 冒烟：pixel-broll / collage-broll 端到端（2026-07-28）

不是一条片，是两个生成型 skill 的首次端到端验证。题材「咖啡因不给你充电，它只是挡住了困的信号」，
两条链各出横竖两条 10s clip，共用同一主题、不共用材质语汇。

- 主调色板（仅 pixel 链用）：`palette/master.png` = midnight-neon 16 色
- 拼贴底色：深绿 #1E4438（色彩语义「认知·系统」），不受主调色板约束
- 留痕：`runs/`（img-state-*.json 三批出图、sd-state.json 四条视频，含 x-oneapi-request-id）
- 证据：`evidence/`（QA JSON + contact sheet）
- 成本：6 张 GPT-Image-2 + 4 条 Seedance 10s ≈ $11

## 结论（已写回两个 SKILL.md 的状态节）

1. **归一层是必需的**：Seedance 原始输出非纯色方块 30.5%（横）/ 49.0%（竖）、越界色 10 万+；归一后 0% / 0 种。
2. **做旧彻底剥离**：锁色判据把画面钉死在 16 色内，`#C9A876` 那类做旧色物理上进不来。
3. **角色一致性靠 `/v1/images/edits` 挂参考图**：首帧归一图当 ref 出尾帧姿态，横竖两条角色、桌椅、台灯、窗户全部对得上。
4. **死尾绝对阈值被证伪**：YDIF<1 是拿拼贴/摄影内容标的，平色像素画在暗底上相邻帧亮度差本就 0.3–0.4，
   四条片误报三条。改为「尾段/全片自身运动量之比 ≥25%」，与内容亮度脱钩，负对照仍开火。
5. **拼贴 16:9 未摊散**，但偏前重（尾段运动量仅全片 27%，竖屏 54%）。
6. 实产时长四条全是 10.042s（请求 10s）。
