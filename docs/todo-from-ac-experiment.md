# A/C 风格包形态实验暴露的待办（2026-07-27）

来源：`whiteboard-generalist`（手写兜底包）vs 现场合成冻结包的两臂对照实验。
下列五条**与 A/C 之争无关**——不管风格包最后长什么样，它们都得修。按优先级排。

实验记录在 scratchpad（`exp-ac/PREREG.md` / `RESULTS.md`），未入库。

---

## ~~P0~~ ✅ 已办（2026-07-27）· G2 评审协议未规定配对还是单臂，而两者能翻转结论

> 落地：`tools/judge/README.md`「评审模式协议」+ `judge.py --mode solo|paired` / `--vs` / `--merge`。
> 三条规则均已可执行：绝对判断缺省单臂（CLI 拦截混用）、相对判断走盲化配对（配对不产出 pass/fail）、
> 两路 `--merge` 出 `decisive|weak|tie|inconclusive`（方向相反 → 无定论）。下游 `styles/_iteration.md`
> 棘轮改判 `ratchet_action`，准入考片制拆成"赢基线走配对 + 过下限走单臂"。用下表数据回放：T1 →
> `inconclusive`，T2 → `weak`。

**证据**：同一批 hero frame，两种评审模式给出相反胜负。

| | 配对路 | 单臂路 |
|---|---|---|
| T1 | A 15 : C 34（C 大胜） | A 23 : C 21（A 微胜） |
| T2 | A 28 : C 32 | A 28 : C 29 |

配对分减单臂分：A-T1 −8、C-T1 +13、A-T2 0、C-T2 +3。T1 的配对评委把两臂朝相反方向拉开 21 分，T2 的几乎没拉开（A-T2 四维一分不差）。

**问题**：`tools/judge/` 现在没说清哪种模式、什么时候用哪种。评审结论因此取决于当时随手选了哪种，这让 `_iteration.md` 的棘轮和风格包准入考片制都失去可比性。

**要做**：把评审模式写进 `tools/judge/README.md` 协议，至少定清——

- 绝对判断（够不够出厂、Golden 下限）必须走**单臂**：有对照物时分数会被对手质量污染；
- 相对判断（新旧版本、A/B 方案）必须走**配对**：跨会话的绝对分不可比；
- 两路都跑时的合并规则，以及分歧多大算"结论不稳、不得声称胜负"。

## P1 · `style-contract@1` 表达不了跨镜头与时间维度的约束

**证据**：四份风格包各自最核心的缝合装置，**全部只能落进 `inviolable` 的自然语言**——

| 包 | 全片最要紧的那道门 | 现状 |
|---|---|---|
| C-T1 | 每镜必须有剖面带；卡钳常驻 | 自然语言 |
| C-T2 | 双版入场时间差 ≤2 帧；两章等长（差 ≤3.0s） | 自然语言 |
| pixel-chronicle | MG 段与实拍段严格交替 | 部分可码判 |
| case-file | 三声部不可删 | ✅ 已可执法 |

能翻译成"谁占多少时长、有几个镜头、有几个 `<video>`"的门都保住了；凡是"某元素在时间上怎么变化""同一件事在不同镜头间是否一致"的门，一条都表达不了。

**讽刺之处**：风格包存在的理由就是跨镜头一致性，而合同格式恰恰表达不了跨镜头一致性。

**要做**（按性价比排）：

1. `plan.chapter_delta_s_max` 之类的**区间算术门**——章节/段落时长相减，是纯算术，最容易补；
2. **跨镜头元素在场性**：某个声明的常驻 overlay 是否出现在每一镜（IR 层可查）；
3. ~~**跨镜头主色漂移**~~ ✅ **2026-07-27 已落地**（详见下）。

### P1-3 落地记录：`render.palette_drift_max`

`measure-render.py` 新增 `palette` 段：逐镜 HSV 粗桶（色相 12×饱和 2×明度 2 + 无彩色 4）主色直方图、两两全变差距离矩阵、`pairwise_distance_var`、每镜 `drift`（到其余各镜的平均距离）与离群镜清单。比较前沿色相轴做环形平滑，免得同一世界里的小幅调色被量化边界放大。执法点在 `gates.py:style_contract_render`，已登记进 `film_ir.contract.ENFORCED_TERMS`。

**标定（这是全片可测缝合的第一个数）**：

| 片 | max_drift | 说明 |
|---|---|---|
| 浣熊 EP01（delivered） | **0.691** | 绿锚。G2 D6 缝合 4/5、⑨.2『质感缝合 ✅』。肇事镜 s17_roof 是夜戏声部的合理色温差 |
| 中国经济 H1（review） | 0.449 | 18 镜里 16 镜聚在蓝调新闻棚，离群的 s11/s05 是没调进去的素材镜 |
| 同片 v2 加框版 | 0.539 | 加框改变了中性区占比 |
| **负对照（合成）** | **0.974** | 把中国经济片一段未调色镜头切进浣熊 [118.3,122.57]。肇事镜 0.691→0.974，其余 24 镜变动 <0.05 |

负对照证明指标**能定位到肇事镜**而不是整片抖动。`pixel-chronicle` 据此定 `value 0.80 / amend [0.72,0.88]`：绿锚留 0.109 余量，红锚在带宽顶仍红。

**遗留债**：

- 只有 `pixel-chronicle` 标定了，且样本 n=1 真片（同包另两部 delivered 片 `uk-argentina-feud` / `dont-set-ac-26` 的成片已不在库，compose 里也只剩 ~100K 的 HTML/字体，重渲要重生成素材）。
- `case-file` 与 `whiteboard-generalist` **未加此条目**——库里没有这两包的成片可测，凭 pixel-chronicle 的数外推正是要避免的拍脑袋。合同键已在 `ENFORCED_TERMS` 里合法，回填时只改 contract.json、不动代码。竖屏案卷的 document 声部（浏览器白）与 typography 声部（深底）天然拉开主色距离，阈值大概率显著高于 0.80，必须实测。
- `max_drift` 对**刻意的夜/昼切换**天然敏感（浣熊片 0.691 正是夜戏拉高的）。当前只能靠把上限放宽来容忍；若将来误伤多，再考虑按声部/章节分组比较而不是全片一个池子。

## P2 · `whiteboard-generalist` 不含色板 hex，下游每次自行发明

**证据**：两个选题的 hero-spec 作者都报告"风格包没给色板 hex，我自己定了一套"。后果在评委那里两题都被抓到——

- T1："橙在第 2 张占半屏，前后彻底消失，它没有语义"
- T2："f1/f2 建立的蓝＝冷链，到结论镜同时是管路、柜体、还有货架里的商品方块，颜色从论证工具退化成装饰色"

**根因**：那份色彩约定只活在单次会话的上下文里，不在任何文件中。所以它保不住跨镜头，更保不住跨会话。

**要做**：给兜底包补一套**语义绑定**的最小色板（不是配色表，是"这个颜色只准表示什么"），以及"选定后写入 `meta.style_notes`、全片只读"的纪律。兜底包的定位是对选题最大限度开放，所以色板本身可以由 agent 现场选，但**必须落文件、必须绑语义、选完即冻结**。

## P3 · `whiteboard-generalist` 不自包含

`styles/whiteboard-generalist/playbook.md` §8 指向 `produce/SKILL.md §1.3` 的开拍提问模板。把包切出来单独用（子 agent 任务卡、评委证据包）就缺一块——本轮 A 臂两个编剧都撞上了，只能按 playbook 点名的决策项自行反推。

**要做**：要么把提问模板抄进包内，要么改成不依赖外部章节号的自包含表述。

## ~~P4 · `collage-broll.md` 全链硬锁 9:16~~（2026-07-28 已过时）

~~已开独立任务（`.claude` 会话内 chip），此处只登记：imagegen size、ffmpeg 首尾帧 `1080x1920`、Seedance `ratio: "9:16"`、两套 prompt 模板的 "vertical 9:16 locked poster frame"。横屏项目没有拼贴声部。~~
被 broll-studio 收敛取代：collage 现为 profile，且横竖屏都有实测原文（`gate2_prompt_16_9` / `gate3_prompt_16_9`，16:9 端到端 2026-07-28 验过）——「硬锁 9:16」与「横屏无拼贴声部」两个前提都不再成立。
